import Layout from '@components/Layout'
import React from 'react'

interface userGuideProps {}

const userGuide = ({}: userGuideProps) => {
  return <Layout>userGuide</Layout>
}

export default userGuide
